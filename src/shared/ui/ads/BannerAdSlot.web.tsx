import { memo } from 'react';

type BannerAdSlotProps = {
  hidden?: boolean;
  disableSafeAreaPadding?: boolean;
};

export const BannerAdSlot = memo(function BannerAdSlot(_props: BannerAdSlotProps) {
  return null;
});
