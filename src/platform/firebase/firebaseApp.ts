/**
 * Firebase 초기화
 *
 * 이 파일은 Firebase 앱을 초기화합니다.
 * google-services.json (Android) 및 GoogleService-Info.plist (iOS)가 필요합니다.
 */

// Firebase는 자동으로 초기화됩니다
// google-services.json / GoogleService-Info.plist 파일을 읽어서 설정을 가져옵니다
export function initializeFirebase() {
  // Firebase는 @react-native-firebase/app 플러그인에 의해 자동 초기화됩니다
  // 추가 설정이 필요한 경우 여기에 작성합니다
  console.log('Firebase initialized');
}
